"""
oauth — top-level package for all OAuth2 provider authentication flows.

Sub-packages:
  common/  — shared RFC 7636 PKCE utilities used by every provider
  google/  — Google OAuth2 authorization, token exchange, and callback handling
  github/  — GitHub OAuth2 authorization, token exchange, and callback handling
"""
