# Personal Workspace Agent

A personal AI agent that will eventually connect to Gmail, Calendar, Drive, and GitHub via Google ADK and FastMCP. Right now this repository contains only the OAuth2 + PKCE authentication layer — plain scripts that prove each provider's login flow works before any abstraction is introduced.
