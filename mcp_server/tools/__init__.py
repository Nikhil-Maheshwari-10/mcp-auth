"""
mcp_server.tools — individual tool modules, one per provider.

Each module registers its tools onto the shared FastMCP instance imported
from mcp_server.server. server.py imports these modules so registration
happens automatically at startup.
"""
