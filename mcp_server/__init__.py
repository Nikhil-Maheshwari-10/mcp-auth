"""
mcp_server/ — FastMCP adapter for Claude Desktop and other MCP clients.

The actual tool implementations live in tools/ (framework-agnostic).
This package wraps them with the MCP JSON-RPC protocol via FastMCP.

Entry point: mcp_server/server.py
"""
